import numpy as np import pandas as pd import torch import torch.nn as nn from statsmodels.tsa.vector\_ar.vecm import VECM, select\_coint\_rank from sklearn.cluster import KMeans # Set random seed for reproducibility np.random.seed(42) torch.manual\_seed(42) # ===================================================================== # 1\. DATA PREPROCESSING &amp; ALIGNMENT (FRED-MD + NOAA CLIMATE DATA) # ===================================================================== def load\_and\_preprocess\_data(n\_obs=300): """ Simulates / loads monthly nonstationary macro-climate state vector z\_t: Endogenous macro: IP, CPI, FFR, Employment (FRED-MD) Climate proxies: Temp Anomaly, PDSI (NOAA) """ dates = pd.date\_range(start="1998-01-01", periods=n\_obs, freq="MS") # Generate nonstationary stochastic trend components (I(1)) e1 = np.random.normal(0, 1, n\_obs).cumsum() e2 = np.random.normal(0, 1, n\_obs).cumsum() # Macro variables sharing stochastic trends (Cointegrated) indpro = 100 + 0.8 \* e1 + np.random.normal(0, 0.5, n\_obs) cpi = 150 + 1.2 \* e1 + 0.3 \* e2 + np.random.normal(0, 0.2, n\_obs) ffr = 2.0 + 0.05 \* e2 + np.random.normal(0, 0.1, n\_obs) payems = 120000 + 500 \* e1 + np.random.normal(0, 100, n\_obs) # Climate proxies (Nonstationary forcing mechanism) temp\_anomaly = 0.5 + 0.01 \* np.arange(n\_obs) + np.random.normal(0, 0.4, n\_obs) pdsi = -0.5 - 0.008 \* np.arange(n\_obs) + np.random.normal(0, 1.2, n\_obs) df = pd.DataFrame({ 'INDPRO': indpro, 'CPI': cpi, 'FFR': ffr, 'PAYEMS': payems, 'TEMP\_ANOMALY': temp\_anomaly, 'PDSI': pdsi }, index=dates) return df # ===================================================================== # 2\. LATENT INTERVENED NON-STATIONARY LEARNING (LIN SURROGATE) # ===================================================================== class LINRegimeClassifier(nn.Module): """ Neural encoder mapping nonstationary climate proxies (Temp, PDSI) to latent environment domain indexes E\_t without assuming Markov chains. """ def \_\_init\_\_(self, input\_dim=2, hidden\_dim=16, num\_regimes=2): super(LINRegimeClassifier, self).\_\_init\_\_() self.net = nn.Sequential( nn.Linear(input\_dim, hidden\_dim), nn.ReLU(), nn.Linear(hidden\_dim, num\_regimes), nn.Softmax(dim=-1) ) def forward(self, c\_t): return self.net(c\_t) def discover\_latent\_regimes(climate\_data, num\_regimes=2): """ Extracts latent regimes E\_t from climate proxies using LIN classification. """ c\_tensor = torch.tensor(climate\_data.values, dtype=torch.float32) model = LINRegimeClassifier(input\_dim=climate\_data.shape[15], num\_regimes=num\_regimes) with torch.no\_grad(): probs = model(c\_tensor).numpy() regimes = np.argmax(probs, axis=1) return regimes # ===================================================================== # 3\. PIECEWISE VECM WITH COMMON ROW SPACE CONDITION (CRSC) # ===================================================================== def estimate\_piecewise\_crsc\_vecm(df\_macro, regimes, k\_ar\_diff=1): """ Estimates regime-specific VECM dynamics while verifying the Common Row Space Condition (CRSC) across regimes. """ unique\_regimes = np.unique(regimes) models = {} alpha\_matrices = {} beta\_matrices = {} # Determine cointegration rank on full sample levels coint\_rank\_res = select\_coint\_rank(df\_macro, det\_order=0, k\_ar\_diff=k\_ar\_diff, method='trace') r = max(1, coint\_rank\_res.rank) print(f"[Cointegration Analysis] Detected Cointegration Rank r = {r}") for l in unique\_regimes: idx = (regimes == l) sub\_df = df\_macro.iloc[idx] if len(sub\_df) &lt; 30: print(f"Warning: Regime {l} has too few observations ({len(sub\_df)}).") continue vecm\_model = VECM(sub\_df, k\_ar\_diff=k\_ar\_diff, coint\_rank=r, deterministic="co") vecm\_res = vecm\_model.fit() models[l] = vecm\_res alpha\_matrices[l] = vecm\_res.alpha beta\_matrices[l] = vecm\_res.beta print(f"--- Regime {l} VECM Estimated (Obs: {len(sub\_df)}) ---") print(f"Alpha Matrix Shape (Regime {l}): {vecm\_res.alpha.shape}") print(f"Beta Matrix Shape (Regime {l}): {vecm\_res.beta.shape}") # Verify Common Row Space Condition (CRSC) # CRSC requires Im(alpha\_l \* beta\_l^T) to share a common subspace dimension across regimes ranks = [np.linalg.matrix\_rank(alpha\_matrices[l] @ beta\_matrices[l].T) for l in models.keys()] print(f"\\n[CRSC Verification] Error-Correction Ranks Across Regimes: {ranks}") if len(set(ranks)) == 1: print("-&gt; Common Row Space Condition (CRSC) Satisfied: Cointegrating subspace rank is invariant.") else: print("-&gt; Warning: Subspace dimension mismatch across regimes. Regularization required.") return models # ===================================================================== # 4\. EXECUTION PIPELINE # ===================================================================== if \_\_name\_\_ == "\_\_main\_\_": # Step 1: Load data data = load\_and\_preprocess\_data(n\_obs=360) macro\_vars = data[['INDPRO', 'CPI', 'FFR', 'PAYEMS']] climate\_vars = data[['TEMP\_ANOMALY', 'PDSI']] # Step 2: Recover latent interventions E\_t via LIN regimes = discover\_latent\_regimes(climate\_vars, num\_regimes=2) data['LATENT\_REGIME'] = regimes # Step 3: Estimate Piecewise Affine VECM enforcing CRSC vecm\_models = estimate\_piecewise\_crsc\_vecm(macro\_vars, regimes, k\_ar\_diff=2)
